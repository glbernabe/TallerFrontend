/**
 * Configuración compartida
 * para todos los overlays.
 */
export const OVERLAY = {

    /**
     * Padding horizontal del contenido.
     */
    contentPadding:
        "px-4 sm:px-6 md:px-8 lg:px-10 xl:px-12",

    /**
     * Separación respecto al Navbar.
     */
    topOffset:
        "pt-[72px]",

    /**
     * Drawer lateral.
     */
    drawerWidth:
        "w-full max-w-full sm:max-w-[560px]",

    /**
     * Buscador.
     */
    searchWidth:
        "w-full max-w-full sm:max-w-[92%] md:max-w-[88%] lg:max-w-[820px] xl:max-w-[860px]",

    /**
     * Tarjeta pequeña.
     */
    cardWidth:
        "w-full max-w-full sm:max-w-[420px]",

};